apiVersion: v1
kind: ConfigMap
metadata:
  name: application-configmap
data:
  application.name: "OTUS Homework 2 Application Name"
  server.port: "8000"
  datasource.user: "root"
  datasource.password: "root"
  datasource.url: jdbc:mysql://mariadb-service/hw2
  datasource.driver: "org.mariadb.jdbc.Driver"
  hibernate.dialect: "org.hibernate.dialect.MariaDB53Dialect"
